# 高考语料（L0 原件层）

## 布局

- `按省采集-版本2/`：31 省采集包（2008-2024，空白卷+解析卷成对，9.9GB/978 件，2026-08-15 入库）。文件夹=采集渠道，**文件名才是卷身份的第一证据**；文件→试卷映射见 `work/knowledge/高考真题整理/EXMAP-V2_版本2文件试卷映射.jsonl`。

## 治理

- 本目录**不入 git**（.gitignore 排除，README 除外）——L0 大文件统一治理专项（设计方案 §12 裁决 4：含 git-lfs 评估）落地前，采取本地保全 + 台账/映射表（入 git）记录的方式；
- 台账（每件 SHA 与裁决）：`Tmp/inbox/LEDGER.jsonl`（本地，Tmp 不入 git）；弃收 36 件=与既有四川库逐字节重复；
- 卷身份规则与分叉契约：`docs/architecture/试卷库组织约定.md`。
