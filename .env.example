# MinerU API token — required for scripts/mineru_client.py and scripts/batch_mineru.py
# 获取地址：https://mineru.net （登录后在个人中心 / API Keys 页面申请）
# 1) 复制本文件为 .env 并填入真实 token，或
# 2) 直接设置环境变量：export MINERU_TOKEN="sk-xxxxxxxx"
# 3) 或写入 ~/.workbuddy/mineru_token（仅本机使用）
#
# 查找优先级：环境变量 MINERU_TOKEN > 项目 .env > ~/.workbuddy/mineru_token
MINERU_TOKEN=your-mineru-token-here
